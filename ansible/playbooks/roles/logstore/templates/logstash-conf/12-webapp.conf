filter {
  if [log_type] == "webapp" {
      json {
        source => "message"
      }
      date {
          locale => "en"
          match => [ "time_local", "dd/MMM/YYYY:HH:mm:ss Z" ]
      }
  }
}