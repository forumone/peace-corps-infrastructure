filter {
  if [log_type] == "webapp" {
      json_lines {
        charset => "UTF-8"
      }
      date {
          locale => "en"
          match => [ "time_local", "dd/MMM/YYYY:HH:mm:ss Z" ]
      }
  }
}